const userName = "gal"

function getUserName(userName: string): string {
    return `${userName}@gmail.com`
}

console.log(getUserName("gal"))



// transpile tsc = ts => js
// run code  node = run js